version: 2

models:
  - name: snowplow_identities_incremental_manifest
    description: '{{ doc("table_incremental_manifest") }}'
    columns:
      - name: model
        description: The name of the model.
        tests:
          - unique
          - not_null
        config:
          tags:
            - primary-key
      - name: first_success
        description: The earliest event consumed by the model, based on `load_tstamp`
      - name: last_success
        description: The latest event consumed by the model, based on `load_tstamp`
